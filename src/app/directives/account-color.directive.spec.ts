import { AccountColorDirective } from './account-color.directive';

describe('AccountColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountColorDirective();
    expect(directive).toBeTruthy();
  });
});
