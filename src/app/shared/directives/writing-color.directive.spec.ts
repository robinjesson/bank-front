import { WritingColorDirective } from './writing-color.directive';

describe('WritingColorDirective', () => {
  it('should create an instance', () => {
    const directive = new WritingColorDirective();
    expect(directive).toBeTruthy();
  });
});
